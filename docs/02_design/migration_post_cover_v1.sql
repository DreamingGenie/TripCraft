-- ============================================================
-- migration: post 테이블에 cover_image 컬럼 추가 (대표사진)
-- 작성일: 2026-06-23
-- 작성자: 송정기
-- 설명: 커뮤니티 "여행 후기" 카드 피드용 대표사진(cover) 저장.
--       우선순위 — 업로드 cover_image > 본문 첫 이미지 > 연결 일정 대표이미지 > null
--       (본문/일정 폴백은 목록 쿼리에서 COALESCE로 해석)
-- ============================================================

ALTER TABLE post
    ADD COLUMN cover_image VARCHAR(500) NULL COMMENT '대표사진 URL (업로드 우선, 없으면 본문/일정 폴백)' AFTER content;
