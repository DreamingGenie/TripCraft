# 기술 심화 — 경로 최적화 & 지도 시각화

> ⚠️ **초안 — 작업자(송정기) 직접 보완 예정.**
> 아래는 AI가 정리한 초안(시작점)이다. 최종본은 작업자가 실제 코드·수치·스크린샷을 더해 완성한다.
> 상세 원문: [`docs/03_dev/transit_external_api.md`](../../03_dev/transit_external_api.md)

---

## 다룰 범위

여러 외부 API를 연동하고 시간표(일정 블록)를 배치하면 **두 장소 사이 최적 이동 경로를 자동으로 계산**하고,
그 경로를 **지도 위에 실제처럼 시각화**하는 기능. 무료 API의 호출 제한을 견디는 다층 캐시가 핵심.

## 1. 외부 API 오케스트레이션

두 장소 사이 "이동수단별 경로" 하나에 **외부 API 4종**(ODsay 대중교통, T Map 자동차·도보, Kakao Local 검색, Naver Maps 렌더)이 얽힌다. 한 번의 호출로 끝나지 않는다 — 도시간(KTX) 경로는 "역→역"만 주므로 **출발지→역, 역→목적지 접근 구간을 추가 호출로 합성**하고, 지도 폴리라인은 별도 호출(ODsay `loadLane`)로 조립한다.

- **경로 보강**(`enrichPublicTransit`): 도시간 경로마다 로컬 경로를 추가 호출해 `[출발지→역]+[KTX]+[역→목적지]`로 합성, 실패 시 하버사인 거리 추정으로 폴백.
- **폴리라인 3단 폴백**: `loadLane`(실폴리라인) → `passStopList`(정류장 좌표) → 구간 양끝 직선.
- **자동차 4옵션**(T Map): 추천/최단/무료/최소를 각각 호출해 비교.

## 2. 다층 캐싱 (핵심 설계)

무료 API는 같은 좌표 반복 호출 시 빈 응답(rate limit)을 준다 →
① `transit_cache`(DB, 좌표쌍·시간대·모드 키) ② `lane_polyline`(DB, 노선 형상)
③ in-memory 좌표 캐시(커스텀 장소) ④ in-memory 로컬경로 캐시(보강 중복 제거).
**성공 결과만 캐시**(빈 응답 캐시 시 영구 빈값 고착 방지).

### 캐시 정밀도 설정 (`system_config`)
`transit_cache_level`(1~5)로 시간대 정밀도 조절 — 레벨1(시간 무시) ~ 레벨5(시간별). 러시아워 경계·레벨별 대표 시간을 `system_config`에 두어 관리자 화면에서 조정 가능. 캐시 키의 `departure_hour`가 레벨에 따라 대표 시간으로 정규화된다.

## 3. 발표 포인트

1. 단일 API로 안 끝남 → 다중 호출 합성.
2. 무료 API의 현실은 rate limit → 8연타 실험으로 규명, 성공만 저장하는 다층 캐시로 해결.
3. 보강 로직을 좌표 기반으로 추출 → 관광지·커스텀 장소가 동일 파이프라인 공유.

## 보완 TODO (작업자)
- [ ] 실제 코드 스니펫(`enrichPublicTransit`, `loadLane` 파싱) 인용
- [ ] 지도 시각화 스크린샷(대중교통 구간·도보·환승 마커)
- [ ] 캐시 적중률·응답시간 측정 수치
