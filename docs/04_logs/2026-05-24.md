# 작업 일지 — 2026-05-24

## 완료

### Frontend: Vanilla JS → Vue 3 마이그래이션

- **Vite 프로젝트 초기 셋업**: `package.json`, `vite.config.js`, `index.html`, `main.js`
  - 의존성: vue 3.5, pinia 2.2, vue-router 4.5, vuedraggable 4.1
  - `/api` → `http://localhost:8080` 프록시 설정 (CORS 제거)

- **Pinia 스토어**
  - `auth.js`: `/api/auth/me` 로 로그인 상태 확인, 쿠키 기반 (localStorage 미사용)
  - `toast.js`: 글로벌 토스트 알림

- **Vue Router**: `requiresAuth` 네비게이션 가드 → 미인증 시 `/auth` 리다이렉트

- **공통 컴포넌트**
  - `AppHeader.vue`: 로그인 상태 반응형 (닉네임 / 로그아웃 버튼)
  - `AppToast.vue`: 토스트 메시지 표시
  - `ScheduleModal.vue`: 새 일정 생성 폼

- **4개 뷰 컴포넌트** (모두 목 데이터 기반)
  - `AuthView.vue`: 로그인 / 회원가입 탭 전환
  - `ExploreView.vue`: 관광지 탐색 (검색 + 필터 + 지도 마커)
  - `ScheduleView.vue`: 시간표 드래그 앤 드롭 (30분 스냅)
  - `CommunityView.vue`: 게시글 목록 / 상세 / 댓글

- 기존 Vanilla 코드 → `frontend/_vanilla/` 아카이브

### 빌드 검증
- `npm install` 후 `npm run build` — 43 modules, 오류 없음
- `npm run dev` — http://localhost:5173 정상 응답 확인

## 다음 작업 예정

- 백엔드 API 연동 (현재 목 데이터)
  - 관광지 조회 (TourAPI 수집 → 자체 DB)
  - 여행 일정 CRUD
  - 커뮤니티 CRUD
- Kakao Maps SDK 연동 (현재 SVG 격자 플레이스홀더)
- vue-draggable 적용 (현재 native HTML5 drag API)
