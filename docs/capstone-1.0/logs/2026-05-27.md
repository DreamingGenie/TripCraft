# 작업 일지 — 2026-05-27

## 완료

### Naver Maps 연동 (Kakao Maps 대체)

- Kakao Maps → Naver Maps 전환 (비즈앱 사업자 등록 없이 개인 계정으로 사용 가능)
- 네이버 클라우드 플랫폼(ncloud.com) Application 등록, Web Dynamic Map 서비스 신청
- `frontend/.env`: `VITE_NAVER_MAP_CLIENT_ID` (JavaScript SDK용 파라미터 `ncpKeyId`)
- `ExploreView.vue`: `loadNaverScript()` → `initMap()` → `updateMarkers()` 구현
  - 한반도 중심(36.5, 127.5) zoom 7 초기화
  - 관광지 필터·페이지 변경 시 `watch(attractions, updateMarkers)` 마커 자동 갱신
  - 마커 클릭 → InfoWindow 장소명 표시
- `vite.config.js`: `port: 5173, strictPort: true` 고정 (Naver 콘솔 도메인 등록 안정화)

### ODsay 이동 시간 자동 계산 (F08 구현 완료)

**백엔드**
- `OdsayClient`: `searchPubTransPathT` 엔드포인트 사용 (`/transit/path` 아님)
  - URI 서비스 키 + `Referer: http://localhost:5173` 헤더 방식 (서버 키의 IP 변동 문제 회피)
  - `URLEncoder`로 API 키 인코딩 처리
  - error 응답이 배열 형태 `[{"code":"500","message":"..."}]` — 배열 파싱 처리
- `TransitServiceImpl`: TransitCache 조회 → 캐시 미스 시 ODsay 호출 → 결과 저장
- `GET /api/transit?fromId=&toId=&hour=&transportType=` 엔드포인트 추가
- `application.yml`: `odsay.api-key` 설정 추가

**프론트엔드**
- `frontend/src/api/transit.js` 신규
- ScheduleView: 블록 배치·이동·삭제 후 `fetchTransitForDay()` 자동 호출
- TransitPill: 블록 내부 뱃지 → **블록 사이 독립 요소**로 변경
  - 위치: 이전 블록 종료 지점(top + height)
  - 높이: 실제 소요 분 = 타임테이블 픽셀(1분 = 1px)
  - 스타일: 사선 패턴 배경, 초록 점선 테두리

### 문서 최신화

- `CLAUDE.md`: 현재 상태 업데이트 (Naver Maps·ODsay 완료), External API Kakao → Naver
- `.claude/plan.md`: ODsay 구현 현황 섹션 추가
